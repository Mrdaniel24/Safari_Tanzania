# Local environment variables (do NOT commit a real .env to the repo)
# Copy this file to .env and fill the real secrets locally.

# Admin credentials (only for local dev/testing). Prefer using a password manager instead.
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme

# Database (optional override)
DB_HOST=127.0.0.1
DB_NAME=safari_tanzania
DB_USER=root
DB_PASS=
DB_CHARSET=utf8mb4
